# Block direct HTTP access to uploaded identity documents.
# Files are served only through the authenticated /file route.
Options -Indexes

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>
